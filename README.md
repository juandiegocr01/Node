#Notas:

```
Esto sólo sirve para crear tablas de multiplicar
por medio de la consola donde ingresas que base quieres,
si quieres imprimir la tabla, hasta que número deseas multiplicar
y guardar un archivo txt con la tabla que desees.
```